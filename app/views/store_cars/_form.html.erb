<%= simple_form_for(@store_car) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :store %>
    <%= f.association :car %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
